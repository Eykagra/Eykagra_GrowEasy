# Local dev: http://localhost:4000
# Vercel Services (both on same domain): leave empty
# Render backend only: https://your-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_BATCH_SIZE=25
NEXT_PUBLIC_MAX_BATCHES=4
NEXT_PUBLIC_PARALLEL_BATCHES=3